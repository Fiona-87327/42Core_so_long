# so_long
so_long is a small 2D puzzle/game project: control a player on a character map to collect all collectibles and reach the exit.
The program (`so_long`) must validate the map, render it, and allow the player to move using keyboard controls.
